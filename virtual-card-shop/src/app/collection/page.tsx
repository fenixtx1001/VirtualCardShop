export default function CollectionPage() {
  return (
    <div style={{ fontFamily: "system-ui" }}>
      <h1 style={{ fontSize: 32, fontWeight: 900, marginTop: 0 }}>Collection</h1>
      <p style={{ marginTop: 6 }}>
        This will show opened cards you own (and later allow selling/trading).
      </p>
      <div style={{ marginTop: 12, padding: 12, border: "1px solid #ddd", background: "#fafafa" }}>
        Coming next: tie pack ripping results to owned cards + show them here.
      </div>
    </div>
  );
}
